# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
bun-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local-only build artifacts and owner backups (excluded from deploys by
# design — deploy.yml excludes isolate/ and backups/ from FTP). These are
# large and must not enter the repo snapshot the platform builds from.
isolate
backups

# Generated source-package artifacts (rebuilt by scripts/package-source.ts)
BUILD-INFO.txt
public/starforce-source-latest.tar.gz
public/starforce-source-latest.tar.gz.sha256

# Legacy stray bundle from the pre-Aug-2026 build
public/Landing-BGCIpYyy.js

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
